apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-set-file.fullname" . }}
  labels:
    {{- include "test-set-file.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-set-file.selectorLabels" . | nindent 4 }}
